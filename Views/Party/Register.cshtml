@model deneme.Models.Guest

@{
    ViewData["Title"] = "Parti Kayıt";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm mt-5">
            <div class="card-header bg-primary text-white">
                <h3 class="card-title mb-0">🎉 Parti Kayıt Formu</h3>
            </div>
            <div class="card-body">
                <form asp-action="Register" method="post">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">İsim:</label>
                        <input asp-for="Name" class="form-control" placeholder="Adınızı giriniz" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Phone" class="form-label">Telefon:</label>
                        <input asp-for="Phone" class="form-control" placeholder="0555..." />
                        <span asp-validation-for="Phone" class="text-danger"></span>
                    </div>


                    <div class="mb-3">
                        <label asp-for="WillAttend" class="form-label">Geliyor musun?</label>
                        <select asp-for="WillAttend" class="form-select">
                            <option value="">Seçiniz...</option>
                            <option value="true">Evet, geliyorum! 🥳</option>
                            <option value="false">Maalesef gelemiyorum 😞</option>
                        </select>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg">Kaydı Tamamla ✅</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>