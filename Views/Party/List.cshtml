<table class="table table-striped">
    <thead>
        <tr>
            <th>İsim</th>
            <th>Telefon</th>
            
            <th>İşlem</th> </tr>
    </thead>
    <tbody>
        @foreach (var guest in Model)
        {
            <tr>
                <td>@guest.Name</td>
                <td>@guest.Phone</td>
                <td>
                    <a asp-action="GuestDetails" 
                    asp-route-id="@guest.Id" 
                    class="btn btn-sm @(guest.WillAttend ? "btn-success" : "btn-danger")">
                        Detaylar 🔍
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>